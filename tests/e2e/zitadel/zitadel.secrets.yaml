# Disposable E2E-only credentials. Production secrets are not committed.
Database:
  postgres:
    User:
      Username: zitadel
      Password: zitadel-e2e-password
    Admin:
      Username: postgres
      Password: postgres
